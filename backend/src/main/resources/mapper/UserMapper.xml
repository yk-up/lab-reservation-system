<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lab.reservation.mapper.UserMapper">

    <resultMap id="UserMap" type="com.lab.reservation.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="realName" column="real_name"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="findByUsername" resultMap="UserMap">
        SELECT * FROM `user` WHERE username = #{username} LIMIT 1
    </select>

    <select id="findById" resultMap="UserMap">
        SELECT * FROM `user` WHERE id = #{id} LIMIT 1
    </select>

    <select id="searchUsersByKeyword" resultType="com.lab.reservation.vo.UserSearchItem">
        SELECT id, username, real_name
        FROM `user`
        WHERE username LIKE CONCAT('%', #{keyword}, '%')
           OR (real_name IS NOT NULL AND real_name LIKE CONCAT('%', #{keyword}, '%'))
           OR CAST(id AS CHAR) = #{keyword}
        ORDER BY id
        LIMIT #{limit}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user` (username, password, real_name, email, phone, role, status)
        VALUES (#{username}, #{password}, #{realName}, #{email}, #{phone}, #{role}, #{status})
    </insert>

    <update id="updateStatus">
        UPDATE `user` SET status = #{status} WHERE id = #{id}
    </update>

</mapper>
